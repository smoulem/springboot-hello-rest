# Simple project

Simple test project

A Rest API that says 'Hello world'

## Run the App:
```shell
./gradlew bootRun
```

Or you can try:
```shell
java -jar build/libs/springboot-hello-rest-0.0.1-SNAPSHOT.jar
```

## Run the tests
```shell
./gradlew test 
```